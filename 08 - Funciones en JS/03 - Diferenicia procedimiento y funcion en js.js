// DIFERENCIA ENTRE PROCEDIMIENTO O METODO Y FUNCION EN JAVASCRIPT

/*
Procedimiento o método -> No retorna ningun valor.

        function imprimir(value){
            console.log(value);
        }

Función -> Retorna algun valor.

        function sumar(a, b) {
            let resultado = a + b;
            return resultado;
        }

        let a = 2, b = 3, c
        c = sumar(a, b);
        console.log(c);

*/


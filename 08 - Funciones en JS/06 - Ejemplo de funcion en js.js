// Funciones en Js

// 1 - Definimos la función.
function sumar(a, b){
    return a + b;
}

// 2 - Llamamos a la función.
let numero1 = 3, numero2 = 2;
let resultado = sumar(numero1, numero2);
console.log(`La suma de ${numero1} + ${numero2} es ${resultado}`);